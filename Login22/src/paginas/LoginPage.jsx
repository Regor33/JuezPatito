import Login from "../components/Login/Login"

const LoginPage = () => {
    return (//<h1>Pagina login</h1>
        <div>
            <h1 className="text-4xl font-bold text-center bg-amber-500">Iniciar Secion</h1>
            <Login/>
        </div>
        )
}

export default LoginPage